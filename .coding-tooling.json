{
  "schemaVersion": 1,
  "profile": "repository-foundation-v1",
  "tiers": {
    "fast": ["format:check", "lint", "typecheck", "build", "test:unit"],
    "full": ["format:check", "lint", "typecheck", "build", "test:unit", "package:check", "benchmark:smoke"]
  },
  "requiredCapabilities": ["format:check", "lint", "typecheck", "build", "test:unit"],
  "optionalCapabilities": ["package:check", "benchmark:smoke"],
  "capabilityCommands": {
    "@moritzbrantner/layer-editor": {
      "package:check": ["bun", "run", "test:package"],
      "benchmark:smoke": ["bun", "run", "bench"]
    }
  }
}
